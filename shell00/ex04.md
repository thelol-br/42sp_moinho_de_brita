# Comandos usados:
```shell
#!/bin/sh
git log -n 5 --format="%H"
```

## git log
Git log lista todo o fluxo da worktree. Usando a flag "-n 5", afunilamos o log para mostrar somente os últimos 5 commits e com a flag --format com %H, mostramos somente a hash por completo destes 5 últimos commits.