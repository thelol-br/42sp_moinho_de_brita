# Comandos usados:
```shell
patch a sw.diff -o b
```

## patch
O comando patch recebe um arquivo original e um arquivo .diff e gera o outro arquivo original que é resultado da subtração do diff pelo primeiro original fornecido.

Usa-se a flag -o para indicar o output file gerado pelo patch.